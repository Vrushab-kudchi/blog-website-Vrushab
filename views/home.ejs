<%- include("partials/header"); -%>

    <h1>Home</h1>
    <p>
        <%= homeData %>
    </p>


    <% postsData.forEach(function(element) { %>
        <h1>
            <%=element.title %>
        </h1>
        <p>
            <%=element.content %>
        </p>
        <% }); %>

            <%- include("partials/footer"); -%>
