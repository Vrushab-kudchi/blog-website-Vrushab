<%- include("partials/header"); -%>

    <h1>Home</h1>
    <p>
        <%= homeData %>
    </p>


    <% postsData.forEach(function(element) { %>
        <h1>
            <%=element.title %>
        </h1>
        <p>
            <%=element.content.substring(0,140) + "..." %>

                <a href="/post/<%= element.title %> ">read more</a>
        </p>

        <% }); %>

            <%- include("partials/footer"); -%>
